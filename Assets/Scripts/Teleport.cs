using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Teleport : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    { if ((Player.transform.position.x > 68 && Player.transform.position.x < 71) && Player.transform.position.y > 0 && Player.transform.position.x < 1))


            }
}
    